# 通用笔记规范

## 1. 核心原则
笔记按执行顺序排列，确保日后打开时能按步骤直接操作，无需额外思考。

## 2. 编号体系
- 大章节：`## 数字. 阶段名`
- 小节：`### 数字.数字 动作/主题名`
- 若某节内容过多需要细分，用 `#### 子主题名`（不加编号），通常两层结构已足够。
- 每个 `###` 下的数字列表从 1 开始重新编号。

## 3. 数字列表规则
- 仅对有明确先后顺序的多步骤操作使用数字列表（1. 2. 3.）。
- 每条数字项只做一件事。如果一条内容包含多个动作，或信息量大到需要读者停下来理清思路，说明需要拆分成多条。
- 拆分的依据是动作数量，而非行数。例如：
  - 错误示范：`1. 分析市场趋势并评估竞品然后确定自己的报价。`
  - 正确示范：
    1. 收集主要竞品的公开信息。
    2. 整理近期的成交案例。
    3. 对比自身与竞品的差异。
    4. 确定初步报价范围。
- 单条内容无论长短，只要没有多个动作，就写成一个自然段落，不用数字。
- 每个 `###` 下超过 7 条数字项时，考虑拆分成更小的子主题。

## 4. 空行规则
- 标题（`#`、`##`、`###`）下方不加空行，直接接内容。
- 数字列表项之间不加空行（表示连续步骤）。
- 段落与段落之间保留一个空行。

## 5. 原因、例子与解释
- 每个观点或步骤必须附带原因（为什么这么做）。格式：`（因为xxx）` 或 `（不然xxx）`。
- 尽可能附带具体例子，格式：`（如xxx）`。例子必须可操作、可想象，不能是空话。
- 如果一个步骤需要读者查外部信息才能理解，说明解释不够完整。

## 6. 加粗规则
- 加粗的目的是降低读者跳过重要信息的概率。只加粗以下情况：
  - 最容易被忽略的核心概念。
  - 关键警示或常见错误提醒。
  - 任何人都会下意识跳过但丢掉它整个步骤就失效的信息。
- 不要用加粗来标注你觉得重要的内容（因为重要性和易忽略性是两回事）。加粗一页里出现四五处以上，说明你还没有真正筛选出最关键的那几句提醒。

## 7. 分节原则
- 每个 `###` 主题必须聚焦于一个明确的动作或决策点，不混杂不同层级的内容。
- 检查方法：如果该小节标题能用“如何……”开头，说明主题清晰；如果标题像“XX与YY”，则需要拆分。

## 8. 目录与锚点
- 笔记开头必须有完整的 Markdown 目录。
- 每个 `##` 和 `###` 标题须设置锚点链接。锚点规则：标题简化英文或拼音，小写，短横连接，编号保留。
  - `## 1. 准备阶段` → `#1-准备阶段`
  - `### 1.1 明确自己的目标` → `#11-明确自己的目标`

## 9. 核验流程
笔记完成后，执行以下核验步骤：
1. 对照源材料：检查每一条结论是否在书中有对应支撑（案例、实验或论证）。无源头支撑的删除或标记为个人补充。
2. 检查执行顺序：从第一步模拟到最后一步，如发现“需要 A 信息但 A 信息要在第 3 步才获取”，说明顺序有误。
3. 检查数字项：逐条问“这条是否只包含一个动作？”如否，拆分。
4. 检查加粗项：逐条问“读者漏掉这句话，这个步骤还能正确执行吗？”如不能，保留；如能，去掉。

## 10. 最终标准
完成后问自己：如果我半年没碰这个领域，今天打开这份笔记，能不能不思考、不查资料、照着做就把事办成？如果能，合格。如果“大部分能但有卡顿”，卡顿处就是需要补解释或重排顺序的地方。